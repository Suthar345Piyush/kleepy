version: "2"
sql:
  - engine: "sqlite"
    schema: "internal/database/migrations"
    queries: "sql/queries.sql"
    gen:
      go:
        package: "database"
        out: "sql"

        emit_json_tags: true
        emit_interface: true