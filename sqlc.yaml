version: "2"
sql:
  - engine: "sqlite"
    schema: "internal/database/migrations"
    queries: "sql/queries.sql"
    gen:
      go:
        package: "database"
        out: "internal/database"

        emit_json_tags: true
        emit_interface: true